export default function helpCommand(bot, msg) {
  const chatId = msg.chat.id;

  const text = `📖 *Список команд*

/start — реєстрація в боті
/help — список команд
/list — список подій
/add — додати подію
/delete — видалити подію

У майбутньому:
- підтвердження присутності
- нагадування перед подією
- кнопки "Йду / Не йду / Можливо"
`;

  bot.sendMessage(chatId, text, { parse_mode: 'Markdown' });
}
